#!/usr/bin/env node

"use strict";

process.bin = process.title = "process-manager";

var app = require("../lib/app.js");
var http = require("http");

http.createServer(app).listen(8001, function () {
    console.log("Running proceger on 8001");
});
